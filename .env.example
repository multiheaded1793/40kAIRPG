MULTIPLAYER_MODE=false
CHATROOM_URL=ws://localhost:8787/ws
MULTIPLAYER_PASSWORD=password
# DM_MODE: polling (default wait loop), manual (no wait), auto (puppeteer injection via CDP)
DM_MODE=auto
AGENT_RULES=["auto-dm-turn.md","start-session.md"]
AGENT_SKILLS=
